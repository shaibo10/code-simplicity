class AdService {
  getAd(adData, type: string) {
    // .. get some data
    // .. map some data
    if (type === 'video') {
      return {
        // .. //
        // .. //
      }
    } else if (type === 'article') {
      // .. get some data
      // .. map some data
      return {
        // .. //
        // .. //
      }
    } else if (type === 'social-network') {
      // .. get some data
      // .. map some data
      return {
        // .. //
        // .. //
      }
    }
  }
}
