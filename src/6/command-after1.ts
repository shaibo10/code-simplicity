class CampaignComponent {
  updateCampaignStatus(campaign, status) {
    switch (status) {
      case StatusState.Archived:
      // do some staff and update the campaign like
      // displayArchiveConfirmationModal
      // updateCampaign,
      // getCampaign,
      // refreshDisplay,
      case StatusState.Paused:
      // do some other staff and update the campaign
      case StatusState.Playing:
      // do some other staff and update the campaign
    }
  }
}
